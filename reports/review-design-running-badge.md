# Folder and group badge correction — accepted

PR18 accepted at `6aefaa59c72f3aa5f2d9dd284cea0d365a111e05`, merged as
`a8663104f97dd5728fab3a8594bb94841d54533e`. Root read the complete final
report, two token changes, comment correction, fixture/probe sources and NOTICE.
Integration `ced70f80` preserves all accepted Pi/drawer source and attribution;
its only conflict was NOTICE. Combined typecheck passes. Worker43 existing tests,
lint, production export and12 after measurements pass at the reported heads.

Root independently reran the actual six-state folder probe through headed
Playwright CLI on4326 at measured export7f3fce05. Normal-text contrast:
light rest/focus6.187, hover5.287; dark rest/focus9.090, hover6.944. Six checked,
zero failures; count and accessible name retained, provider4→4. Evidence in
`browser-phase1-final/badge-recheck*` and `badge-after-*.png`. Root browser closed.
Initial unauthenticated workspace navigation produced expected session errors;
actual login then succeeded. Existing fixture-folder/Pi diagnostics remain;
this is not a zero-console claim.

The group counterpart is a real workspace route component. Its worker-only
browser response fixture overlays three read-only list results to supply a
synthetic group, without modifying stored data. Root reviewed that trace,
identical six measured ratios and cleanup restoring zero groups. No independent
root group-interception rerun is claimed. No provider/model action occurred.

Both10px badges use installed amber800 in light mode, replacing amber700.
Dark colors, count, focus/hover behavior, sizes and visibility semantics remain.
Source-only whole-component lint advisories are retained with scope notes;
actual Tailwind hover selector has its media guard. Final combined design
review and root native/export validation follow separately.
