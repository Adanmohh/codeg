# Independent E1 implementation review

**R1–R3 are closed for the reviewed internal helpers at `9949c8a9ad99ffb70232c05bf1c2ad243dcaf194`: independent source review and all 14 unchanged correction/control tests pass, with no additional blocking finding in this bounded scope.** No runtime launch, route, import/publication or tenant/native isolation acceptance is implied. The [independent execution report](business-ai-execution-review/9949c8a9/execution/review.md) records exact commands, raw logs, limits and preservation evidence.

| Independent selector | Result | Cargo compile / test runtime |
| --- | --- | --- |
| `execution_pagination_` | 3 passed, exit 0 | 2m16s / 0.29s |
| `execution_admission_` | 3 passed, exit 0 | 0.97s cached / 0.29s |
| `execution_authority_` | 3 passed, exit 0 | 0.53s cached / 0.26s |
| `execution_assets_` | 5 passed, exit 0 | 0.62s cached / 0.10s |

The four commands ran sequentially with `--locked --offline --no-default-features --lib -j 2` in the existing `.build/business-integration-review/target-15bb402b`. All 802 staged Git blobs match before and after execution, including manifests, lock, tests and the reused vendor source. No reviewer test patch or assertion adjustment was needed. The first Cargo process started at 2026-09-09 15:44:08 UTC with 15.829 GiB free; every prefix started above 10 GiB. All commands finished, and root/tickets received the build-window release at 15:46:43 UTC with 14.867 GiB free, before this publication. Seven test-build warnings and the future-compatibility notice remain in the raw logs. No extra schema, server, native, export or broad A gate ran.

The original three P2 findings against `3773a027a8d580bd9bac1808efdb718ae6f9e135`, published at `dc0e9cb06ded25122c373029c3bc9f9bbe40b42f`, remain unchanged in [their report](business-ai-execution-review/3773a027/review.md) and [ledger](business-ai-execution-review/3773a027/source-ledger.json). The source-only correction assessment and its then-pending execution status are preserved at checkpoint `42d5978b2fde232b8c066131407323955148ed71` in [review.md](business-ai-execution-review/9949c8a9/review.md), [source-ledger.json](business-ai-execution-review/9949c8a9/source-ledger.json) and their original digest index. Verified owner evidence at `b15ded12cd24f557d0de9bb55b43db30b9f8bc89` remains separately attributed. The earlier f4 schema evidence is unchanged; its historical review follows below.

This execution used only fresh, exact source staging and the previously authorized reviewer compiler target. No dependency, target or build output was copied; the unchanged path dependency was reused by a verified symlink. Product files, old source archives, fixtures, browsers, bundles and paused work remain unchanged. Normal Git and immutable `gh api` succeeded in this session. No enforcement hook was installed, restored, probed or claimed. The earlier hook discussion below is historical.

## Frozen schema/DTO verdict — 177d0f3e

No blocking findings in the implemented schema and closed DTO scope at `177d0f3e2b426d65de9573ba534ac8c11f9250f7` ([PR32](https://github.com/Adanmohh/codeg/pull/32)). Independent checks passed **11 tests, zero failures**. E1 routes, launches, runtime authority, file import and publication consumers are absent from this frozen source and are not accepted by this verdict.

The complete initial product `6195d9daf049b7cd16615e35b87d08c7f2870bf0`, owner report/evidence `fdfe342940d1a6881005e5534b3f0090f9391ef7`, and combined delta/report were read. Own `gh api` resolves177d0f3e to tree `1c4e7f0a6e56075329a2820b32edebfd73edf89c`, parent `b73940560a69748f6774f41eb1a8de44e7c4c11f`. The combined change adds accepted B source and improves two E1 tests; production E1 Rust/SQL/DTO/validation is identical to6195. Applied migrations10/11/12/13, Cargo manifest/lock and LICENSE match accepted B `c8453a48d441f40eb47f9c9af856235b4f9986e5`. Registration orders11,12,13,14.

## Implemented source assessment

- Migration14 retains every deliverable column and composite FK, restores immutable history triggers, and changes only the text-length lower bound for future file-only submissions. Existing text submission still rejects blank text. One pinned SQLx connection disables FK enforcement outside `BEGIN IMMEDIATE`; DDL/data and the completion marker commit together. A missing separate SeaORM receipt can retry without rebuilding. Errors roll back before FK restoration; cancellation marks the leased connection for closure.
- Session authority, original actor, captured tenant/task epochs and profile revision are immutable. Assignment/owner/reviewer/domain/archive and done/cancelled transitions advance task scope and revoke old generations. Profile changes also revoke sessions. These triggers do not prove future runtime consumers revalidate original authority.
- Operation identity, payload and lineage cannot be updated; confirmed/failed receipts cannot be updated or deleted. Managed versions and selected publication rows are immutable with composite task/tenant references. Runtime transitions, stable file bytes and atomic task/publication/receipt consumption still require implementation and tests.
- Caller DTOs reject unknown fields and contain no actor/org, command, environment, credential or filesystem path input. Private producer metadata and public selected-version metadata are distinct types. Prompt receipts include messageId/inputHash. `account_snapshot` is explicitly unavailable. NDJSON types alone do not prove stream bounds, replay order or current-authority delivery.

## Independent execution and preservation

Owner evidence at177d0f3e records four schema cases: populated human history/real receipt retry; mid-DDL rollback with populated deliverable; every-column synthetic execution link/grant/epoch and agent deliverable retention; closed input validation. Those execution rows are schema fixtures, not live engine admission proof. Evidence-only `25badf45db086c3dd33d841d55175f724108c6cc` records four B epoch migration passes and integrated server check. These remain owner results.

All three commands used the frozen source archive, the existing reviewer target, `cargo test --locked --offline --no-default-features --lib -j 2`, and the selectors below. Exact absolute commands, tool-confirmed exits, timings and log hashes are in [results.json](business-ai-execution-review/177d0f3e/results.json).

| Selector | Independent result | Cargo compile / test runtime |
| --- | --- | --- |
| `business_execution::tests` | 4 passed, exit0; unchanged source | 1m59s / 0.27s |
| `business_intake::tests::epoch_migration_cases` | 4 passed, exit0; unchanged source | 1.05s cached / 0.64s |
| `business_execution::tests::reviewer_schema` | 3 passed, exit0; reviewer-only additions | 1m10s / 0.36s |

The unchanged cases cover populated original deliverable/execution/authority retention, rollback, real migration receipts, both B installation orders, historical NULL epochs and closed input validation. The three [reviewer probes](business-ai-execution-review/177d0f3e/reviewer_schema.rs) add:

1. A second SQLite connection holds `BEGIN IMMEDIATE` while migration14 owns the only leased migration connection. Abort is confirmed; the replacement connection loses the temporary identity marker and enforces FKs. No partial migration marker/receipt appears. The exact task/detail/deliverable survives cancellation and a successful real Migrator retry.
2. Real provisioned tenants and distinct same-tenant tasks/members supply existing positive and negative references. Fifteen rejected FK/immutable writes leave complete snapshots of seven new tables unchanged. Correct profile/session/generation, asset/version, publication and receipt rows remain intact through real receipt loss/retry. Assignment away/back and cancel/reopen advance task scope without reactivating revoked sessions; profile revision changes revoke a fresh session. These are schema fixtures, not an E1 permission or launch test.
3. Valid nested prompt/selection and public-version shapes pass; nested caller authority/path/hash fields and private producer metadata fail deserialization. A multibyte message at16KiB passes and one extra character fails; terminal byte boundaries also pass. No live stream or protected public route is exercised.

There was no failed test iteration or assertion adjustment. Existing linker unwind-size and proc-macro-error2 future-compatibility warnings are retained in the logs, not suppressed. Unfiltered staged `git diff --check` exits2 for raw log terminal blank lines and the unified patch's empty context line; their exact bytes are preserved. The documentation/Rust evidence check excluding those four raw artifacts passes. No additional broad A suite, server/native build, browser or provider test was run by this reviewer.

[source-before.json](business-ai-execution-review/177d0f3e/source-before.json) matches780/780 immutable blobs. [source-after.json](business-ai-execution-review/177d0f3e/source-after.json) matches779/780 originals: the sole difference is the reviewer module registration in `business_execution/tests.rs`, plus one added reviewer file. Every production file and lock remains unchanged. The [reproducible patch](business-ai-execution-review/177d0f3e/reviewer-probes.patch), three exact logs and [digest index](business-ai-execution-review/177d0f3e/digests.json) are committed. Five immutable owner log digests were independently verified and remain separately attributed in [owner-digests-verified.json](business-ai-execution-review/177d0f3e/owner-digests-verified.json).

Only source was extracted to `.build/business-integration-review/177d0f3e/source`; the existing target is `.build/business-integration-review/target-15bb402b`. No target/output was copied or removed. Space observations were5.5GiB before,4.7GiB after the unchanged tests, and3.9GiB after the probes. All processes finished and the build window was explicitly released to tickets/root. All fixture listeners, user browsers, old archives, paused reports and native artifacts are unchanged.

## Runtime prerequisites, not findings against this checkpoint

The next implementation must enforce the retained original operator/credential/tenant epoch before and after async work, exact session/generation/profile ownership, bounded admission/events and uncertain-operation recovery. Generation/event/output rows require guarded writers; this verdict does not call every column in those tables immutable. File custody, read-once immutable bytes, path escape/alias rejection, import recovery and human-only task/publication/receipt CAS remain untested because they do not exist here.

`business_execution/types.rs:346` wraps existing task Detail in SubmitResult. At this pin, `business_tasks/types.rs:222` still has the legacy text-only Deliverable without `assets`. The accepted selected `PublishedAssetRef[]` projection and historical `[]` must accompany task-owned publication/read integration before claiming reload rediscovery. This scoped dependency and the guarded-writer requirement were relayed to tickets; the owner confirms they are in progress on later unreviewed source. E2 tenant execution/native isolation and E3 account snapshots remain separate, unavailable capabilities.

## Docs-first and provenance

Updated global AGENTS and docs-first preface were read. Actual own-session Git, immutable `gh api` and authorized Herdr commands succeed after the owner update. The previous blocked report is retained verbatim as [historical-hook-block.md](business-ai-execution-review/177d0f3e/historical-hook-block.md). A malformed delete/add report patch was denied `patch_target_changed`; the unchanged file was reread and corrected through the same native patch tool. No denied edit was rerouted.

Turn context line2814 at2026-09-09T13:05:16.458Z records `gpt-6-astra`, `max`, `never`, `danger-full-access`, correct worktree and session `01a084ee-129a-7561-9f10-64266115e759`. Old audit lines27340/27341 predate this resumed window. Root confirms the current launcher emits no generic Pre/Post JSONL and the rollout has no hook event_msg entries. These are not relabelled as live hook events. A read-only engine-path search was denied `reader_arguments_invalid`; it was not rerouted. Actual command successes and denials establish observed behavior only. No protected hook state/key was inspected or modified.

The code-context skill ran with existing rag-skills Python and `HF_HUB_OFFLINE=1`: guide exit0; installed-doc lookup exit3 because `data/code/approvals.db` is absent. No corpus coverage or install is claimed. Generic SQLite/defensive-boundary guidance was considered; unrelated per-tenant database/delegation rules do not override the accepted shared database and no-workers assignment.

Installed source read: SeaORM/migration1.1.19 (`migrator.rs`: SQLite migration and separate receipt; pool size configuration); SQLx0.8.6 (`pool/connection.rs`, `transaction.rs`, SQLite connection: close-on-drop, queued rollback and BEGIN IMMEDIATE); serde/derive1.0.228 (`de/struct_.rs`, `enum_internally.rs`: unknown/duplicate fields). Tokio1.49.0 JoinHandle documentation grounds explicit abort/await confirmation; tempfile3.24.0 documents isolated directory cleanup. Cargo pins UUID1.20.0. Exact files/ranges and hashes are in [installed-source.json](business-ai-execution-review/177d0f3e/installed-source.json). No package or source version was upgraded. The reviewer adaptation has its own supplementary [NOTICE](business-ai-execution-review/177d0f3e/NOTICE).

Apache2.0 LICENSE was read. Official `gh api` tree at base `9e61fe67261219921e0a6a860c45f6e08ea99c21` verifies NOTICE sources: migration12 blob `a305a682bbf55e750a5d5156ac0aa4695aac08c1`; migration10 `3cb107d5bc0ab375e1b82fc64c7fefc82396b318`; task store `d48fdf1e85feb4410dbff51e8a671d6c9d525691`; identity module `f7205760715d2516b11b6e2cb79fd133218672bb`. Complete migration10/12 source was compared with14. Existing NOTICE entries are retained; this reviewer added no AGPL/GPL/proprietary source. Upstream remains Codeg v0.30.4 / `6f6bd648b206412644842a98d9ffeebf57292bed`.

Reviewer branch: `review/business-integration`; early report checkpoint `bb33fbffb1e3ddf149cda344a6bfd214b59d79d8`. Pushed recovery evidence `c0c7e522600a94bcf33674f640d5f3665cffd39b` and its31 new evidence digests remain intact. This final report/evidence are the only new committed paths. The containing commit is relayed to root/tickets for immutable import. No additional schema rerun is planned; next runtime review starts only from its supplied committed head.
