# Business intake contract preparation

Docs-only checkpoint; Increment B is **not implemented**. Branch
`docs/business-intake-contract` starts from current accepted main
`c7f7366fef2d7945cea18d4da1b7594fa3026e1c`. Deliverable contract:
[docs/contracts/business-intake.md](../docs/contracts/business-intake.md).

Read complete FOUNDING, ORCHESTRATOR, STATUS, DECISIONS, AGENTS, implementation
scope, all three prior research reports and root's business research synthesis,
plus accepted identity/task contracts. Re-read actual Principal/write/create/
capability/HTTP source before proposing integration. Source inspection confirms
task create owns its transaction today; atomic candidate acceptance needs a
narrow task-owner helper extraction, not duplicated auth/SQL or two HTTP writes.

Independently read official Fireflies adapter queries/transport/LICENSE through
`gh api` at `fbd24607bc784a2294ce402426aefe2cb8c00f50` (all exit0). Keep the
reviewed pin; no SDK/runtime adoption. Source access must remain separate from
domain-public tasks, and existing Ops account authorization cannot be inferred
from business membership.

Remaining bounded preparation: verify adjacent Fireflies query arguments/error
and credential contracts, approved IntroMail provenance, existing email/Hafidh
link/read seams; complete exact DTOs, durable claim/reimport rules, transcript
visibility and concrete synthetic acceptance cases. Root reviews the completed
contract before any B product implementation.

Checkpoint before the root-requested final N2 artifact recheck: those adjacent
source reads are now complete and the contract draft includes closed DTOs,
authenticated resumable claims, publication/source separation and 18 proposed
synthetic cases. Its final consistency review and exact source ledger/report
handoff remain. No B implementation or tests have run. The separate companion
report/result update will preserve this docs branch and the previous artifact
evidence, then this bounded contract preparation resumes.

Docs-first code-context guide used the existing venv with `HF_HUB_OFFLINE=1`,
exit0, mostly other-project rules. No applicable new code rule was invented;
user's no-extra-worker limit overrides unrelated delegation guidance. Installed
docs retrieval exited3 because `tickets.db` is absent. Local source and CLI help
are the authority; no ingestion/install. Initial large read output was truncated;
required documents were reread in bounded complete chunks.

Tracked handoff was clean; paused untracked `reports/visual-correspondence.md`
did not collide with main and remains untouched. No product/NOTICE/lock/config
change, runtime launch, provider request, build/test, target/output mutation,
fixture/browser change or new worker. Only these two Markdown files are added.
