# Phase 1 Design Studio review — accepted

Audited against `docs/design/BRIEF.html`, schema1, scan mode. Specialists:
aesthetic-judge, a11y-auditor and flow-validator methods, applied sequentially
by one Astra/max worker. Root independently reviewed source, measurements,
post-interaction images and protected browser flows; this is not three
independent judges. Local Design Studio source:55c8614d.

## Scope and evidence

The Phase1 Ops inbox/thread/composer/review/Morning, intake/proof/issue/phone
review and relevant connection/sidebar/Pi setup surfaces. The inherited task
engine, terminals and unrelated settings/routes are not exhaustively audited.
The specialist's BC1–18 matrix ties each assertion to concrete screenshots,
real protected fixture APIs and provider counts, with limits per assertion.

Root read the complete18-item matrix and specialist findings. Original71
captures contain2188 text samples. Final four settled drawer probes add268
ranked samples, with no contrast/name/overflow failures in those samples.
Both badge implementations pass12 measured interaction states after correction.
Root independently repeated six folder states and the final integrated mobile
drawer opening/Escape/outside paths, supplementing earlier full payload,
locale, receipt and phone checks.

## Findings and corrective loops

- Locale-change draft loss, RTL/back direction, terminal copy, selected metadata,
  shell labels/focus/contrast and Pi setup presentation were corrected in the
  accepted earlier loops (PRs13/14 and login correction in PR10).
- Both10px running badges failed light rest/focus4.39 and hover3.75. PR18 uses
  existing amber800:6.187 rest/focus,5.287 hover. Dark9.090/6.944 remains.
  Root independently verified folder results; the final specialist independently
  repeated folder and labelled group-response fixture results, then removed it.
- The mobile drawer slid341.5px for450ms under reduce. PR19 removes popup
  transitions only under that preference. Actual frames now contain no
  intermediate travel/animation; normal transitions, focus, Escape/outside,
  nested dismissal and direct swipe behavior remain. Root and specialist final
  integrated CLI runs pass. Duration-only lint still reports false; its raw
  output remains, with the actual property/frame evidence explaining why.
- Login lacked a main landmark. PR20 changes only the outer div tags to main.
  Root and the specialist independently verified exactly one main containing
  the heading/form/error at 390 and 1280px, invalid-token feedback and successful
  keyboard retry. Both final protected browser runs pass.

No unverified high finding is promoted. First-frame transparent focus and
mid-dismissal occlusion screenshots were rejected as passing evidence, preserved
as attempts, then replaced by settled visible ring/popup-detached checks.

## Functional interpretation

All18 bounded checklist assertions pass with their stated limits. Actual notes
stay private; invalid drafts retain text; cancelling navigation preserves edits;
locale and viewport changes preserve in-memory payloads. Complete email/issue
reviews remain human-gated. A saved newer draft invalidates old review. Terminal
copy distinguishes provider acceptance, rejection, denial and unknown outcomes.
Finish recording saves locally with provider4→4. Phone issue creation is exactly
once; unknown reconciliation adds no create request; used locators invalidate.

Missing proof lists build/screen/reciter/log and prevents preparation; invalid
proof is rejected without creating a proposal/filing. Empty and populated
Morning/inbox states show actual available work/actions. The unconfigured Resend
capture also has a stale draft; it proves the visible connection guidance, not
an isolated fresh-proposal preflight. Earlier accepted API/browser evidence
supports that separate gate. Synthetic fixtures are not live beta evidence.

## Measurement limits

Original brief values, probe failures and lint advisories are preserved.
Canvas conversion handles authored OKLCH versus rendered hex; source-backed
dark pairs, alpha composites, inherited radii and font fallback stacks explain
remaining arithmetic/string flags. Token provenance alone never exempts real
contrast failures. Closed details require native checkVisibility; empty-valued
selects still paint their selected option and remain sampled.

This review does not certify whole-app WCAG conformance, screen-reader use,
native WKWebView/physical touch, every custom theme, or document-start CLS/jank.
Ops copy remains English within the supported RTL layout. Live Hafidh/App,
Resend, Telegram reachability and Astra/pi-acp setup are separate unresolved
configuration/evidence limits, not represented by synthetic passes.

## Final acceptance

PR #16 handoff `6419bd87678ad25b7a1c997833c745cd3ec07c47` is accepted and merged
as `8bb49c92d34ac8c8e4aeda6ab7abf54b55b0fa89`. Its only source changes isolate
three existing ignored manual fixtures by loopback port/export; no runtime
product changes follow the final app build. Two trailing-blank-line warnings
remain in preserved raw selector-error transcripts.

Root ran Design Studio's `scripts/merge-findings.mjs` against the final
`aesthetic-judge.json`, `a11y-final.json` and `flow-validator.json`.
[Mechanical output](design-phase1-merged.json): deduped findings and top fixes
are both empty. No high, medium or low finding remains verified in the covered
states; this does not remove the coverage limits above.

| Dimension | Score /10 |
| --- | ---: |
| States | 9 |
| Feedback | 9 |
| Accessibility | 8 |
| Responsive | 9 |
| Visual/tokens | 8 |

Root accepts the specialist's qualified **8.1/10** synthesis: Design 8 × 40%,
Usability 8.75 × 30%, Creativity 7 × 20%, Content 9 × 10% = 8.125.
The score describes this operational UI's measured scope, not whole-app
conformance or an independent panel verdict. Reviewer memory has been updated
with the resolved findings and measurement lessons.

[Native build/startup](native-phase1-build.md) and
[independent browser evidence](browser-phase1-final/README.md) document the
final artifact and root rechecks, including the settings capture incident.

