# Independent P1 browser review

In progress, 2026-09-08. Actual headed Playwright CLI session
`ops-bug-independent`, protected loopback fixture4322 owned by rebrand.
Synthetic token `ops-intake-synthetic-operator`; no live provider credentials.

Login and Bug intake navigation passed. Selecting synthetic-unconfigured
shows missing Hafidh credential/App key in a status region, disables the read
action and opens the correctly bound settings form. An attempted click on
the disabled read timed out as expected; subsequent snapshot verifies the
intended disabled state, not a failed request. No settings were saved.

At390×844, settings retain product selection across the shell remount. After
closing the inherited sidebar drawer, measured document width equals viewport
width390. All11 visible input/select/textarea controls have associated labels.
The mobile screenshot was inspected; form content remains readable/scrollable.
Full evidence/filing/receipt/fix-task flow remains pending.

## Released fixture review

After owner released all three records, root read the real TestFlight list.
FAX had an existing pending review plus earlier failed receipt; the list
invalidated freshness and disabled filing. Refreshing the unchanged source
restored its exact review; approval remained disabled until the explicit human
checkbox was checked. Root inspected repository/title/body/labels and approved
the synthetic request, which reported the deliberately rejected provider
attempt. No live issue was requested.

FAV showed the owner's created synthetic issue receipt and linked held task2.
Review in Tasks opened the actual task list, and task detail showed Canceled,
report-first instructions, fenced external issue content and manual Requeue.
Root inspected the screenshot and did not requeue or start an engine/model.
Unknown-to-created reconciliation was already exercised by the owner; root
does not claim an independent repeat of that browser transition. Rust host
regressions cover the durable transition independently.

Root also independently passed all8 frontend tests on the final pending-proof/
stale-payload correction: `/tmp/ops-bug-independent-frontend.log`,exit0.
