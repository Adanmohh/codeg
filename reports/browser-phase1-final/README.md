# Root final browser and artifact evidence

Actual Playwright CLI0.1.18, accepted product through a8663104/4611d025.
Final root export produced by native build at2fb83797; actual protected server
on127.0.0.1:4318 with existing isolated test data. No live provider actions.

- `drawer-recheck.json` and script: real requestAnimationFrame/getAnimations
  measurements. Under reduce,0 animated frames for opening/Escape/outside;
  no-preference retains28/25/24 animated frames respectively. Both preserve
 331.5×828 popup geometry in390×844, keyboard focus entry/return and dismissal.
  Scripts assert the final utility, preference, focus and overflow; images inspected.
- Empty Ops390 and Morning390/1280 show actual unconfigured/empty states,
  available actions and no horizontal overflow. A guessed Morning heading did
  not match; the subsequent assertions use the actual rendered empty-state copy.
- `badge-recheck*`: separate root rerun on the owner's4326 export7f3fce05,
  six light/dark/rest/hover/focus samples pass, provider4→4. No group response
  overrides in the root run. Its browser was closed; attribution in the badge review.
- `bundle-web-verification.json`: final app's1006 web files exactly match out/.

The initial backend-upgrade screenshot predates the final export and remains
labelled as that checkpoint. Final screenshots are a bounded browser review,
not live beta delivery or native WebView automation.

One root Settings inspection unexpectedly exposed an existing local credential
in CLI snapshot output because the inherited settings surface reads local agent
configuration. Root stopped that capture, removed its owned generated snapshot
and closed the settings tab. No configuration value was copied into report
evidence or changed. A credential-shape scan of final text evidence and all
tracked reports found no matches. Future settings probes must select field names
and public test values only; the tool transcript exposure cannot be retracted.
