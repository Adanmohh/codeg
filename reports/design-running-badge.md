# Running badge contrast

Complete and ready for root review in draft [PR18](https://github.com/Adanmohh/codeg/pull/18). Final tested product/export commit: **7f3fce05092ecbd62525fac7cf0788d62efae9e5**; closing changes are report metadata only. Worktree `/Users/mohamedadan/projects/_worktrees/ops-desk/rebrand`; branch `fix/design-running-badge`, clean-check/fetched accepted base **49e75d19483dc4b6851b22e04d73b66326c9aa47**. Product changes are `text-amber-700` → existing `text-amber-800` in the folder running-count badge (`sidebar-conversation-list.tsx:506`) and its live group counterpart (`sidebar-folder-group-header.tsx:206`), plus the folder's inaccurate contrast comment. Count, zero-count omission, title/screen-reader label, sizing, hover/focus behavior and both dark utilities remain unchanged. No other product, dependency, lockfile or protected-document edit.

**The group header is live, not unused.** `layout/sidebar.tsx:617` renders `SidebarConversationList`; its `folder-group` row branch at line 2780 renders `SidebarFolderGroupHeader` with `groupRunningCount` (line 2620, summing member-folder running sessions). `app-workspace-store.ts:342` loads folder/group lists together. The existing 4326 database has no groups, so the [browser-only setup](design-running-badge-group-setup.playwright) overlays the responses of the three existing read-only list RPCs (`list_folder_groups`, `list_open_folder_details`, `list_all_folder_details`) with **Synthetic badge group**, containing its existing folder. These inherited reads use POST transport; no create/update/group-assignment RPC or database write was performed. [Actual route evidence](design-running-badge-group-setup.raw) shows `/workspace` rendering the real group component and **6 sessions running**. [Cleanup](design-running-badge-group-cleanup.raw) removes all three overrides, reloads real server data and verifies zero groups, the original folder/count and provider **4**.

Baseline actual Playwright CLI measurements on the released existing 4326 fixture confirm light failures: **4.39:1 rest/focus**, **3.75:1 hover**. Root's **4.38:1** used fractional effective-background channels; this probe uses Canvas 8-bit pixels, yielding RGB(250,238,220) at rest. The conclusion is identical. Installed amber-800 produces RGB(151,60,0), measuring **6.19:1 rest/focus** and **5.29:1 hover** on the same actual backgrounds. This candidate was measured before the product edit. Baseline dark passes at **9.09:1 rest/focus**, **6.94:1 hover**.

Actual rebuilt-export measurements at **1280×900** confirm **six checked per badge, twelve passing after samples**, using the same [CLI script](design-running-badge.playwright) and [Design Studio formatter](design-running-badge-report.mjs). Both stay **10px**, weight **600**, checked against normal-text **4.5:1**. Raw folder [before](design-running-badge-before.raw) / [after](design-running-badge-after.raw) and group [before](design-running-badge-group-before.raw) / [after](design-running-badge-group-after.raw) retain real computed styles, ancestor backgrounds, focus-visible states and accessible names. The probe rejects unaccounted ancestor opacity or background images. Initial folder evidence at **b531501e** remains valid: its product code is unchanged in the final group correction. Group measurements use the final rebuilt source. Both components independently produced the following identical ratios:

| Theme/state | Before | Rebuilt after | After foreground / effective background |
| --- | --- | --- | --- |
| Light rest | 4.39 | **6.19** | RGB(151,60,0) / RGB(250,238,220) |
| Light hover | 3.75 | **5.29** | RGB(151,60,0) / RGB(233,221,202) |
| Light keyboard focus | 4.39 | **6.19** | RGB(151,60,0) / RGB(250,238,220) |
| Dark rest | 9.09 | **9.09** | RGB(255,210,48) / RGB(58,47,20) |
| Dark hover | 6.94 | **6.94** | RGB(255,210,48) / RGB(76,65,37) |
| Dark keyboard focus | 9.09 | **9.09** | RGB(255,210,48) / RGB(58,47,20) |

All **24 screenshots** are in [browser-running-badge](browser-running-badge/). Directly inspected examples: folder [before light](browser-running-badge/before-light-rest.png), [after focus](browser-running-badge/after-light-focus.png), [after dark hover](browser-running-badge/after-dark-hover.png); group [before light](browser-running-badge/group-before-light-rest.png), [after focus](browser-running-badge/group-after-light-focus.png), [after dark hover](browser-running-badge/group-after-dark-hover.png). Keyboard Tab entry verifies `:focus-visible` and the inherited inset ring in both themes. Accessible names/expanded states, counts and hidden text/titles are preserved. No folder/group activation was needed. Design Studio [folder after](design-running-badge-after-measured.json) / [group after](design-running-badge-group-after-measured.json) each have zero contrast failures; both before artifacts retain the failures.

Source mapping: exact inherited badges were read from Codeg **v0.30.4 / 6f6bd648b206412644842a98d9ffeebf57292bed**, `src/components/conversations/sidebar-conversation-list.tsx:491–519` and `sidebar-folder-group-header.tsx:193–224` (Apache-2.0). The existing **tailwindcss@4.1.18** `theme.css:33–44` defines all amber tokens; no new palette or dependency source is imported. NOTICE records both files, retaining the original Apache LICENSE and every existing notice. Probe glue adapts the accepted `reports/design-ops-probe.playwright`; the formatter imports local Design Studio **55c8614dcfff33b4caa5a544b4f1f91877214878** `lab/tools/probe.mjs` functions. Group fixture DTOs/read semantics came from accepted `src/lib/types.ts:361–420`, `api.ts`, workspace store, web transport/router and `commands/folders.rs`/`web/handlers/folders.rs`; installed **playwright-core@1.63.0-alpha-2026-08-05** types ground `route`, `fetch`, `fulfill` and `unroute`. No AGPL/enterprise/PolyForm source.

Docs-first: complete founding/orchestration/status/decisions/AGENTS documents and applicable code-context/Playwright/design-audit methods were read. Code-context ran through the existing RAG `.venv/bin/python`, `HF_HUB_OFFLINE=1`: guide exit 0 (measure first; actual headed browser checks), dependency docs exit 3 because `data/code/rebrand.db` is absent. Direct installed source supplies that missing coverage; no corpus or environment changes. Additional local authority: **next-themes@0.4.6** media-query handling and existing `defaultTheme="system"`; **TypeScript@5.8.3** Canvas/DOM types; **@types/node@25.2.2** `fs.d.ts` UTF-8 `readFileSync` overload (types, not the Node runtime version); installed **Playwright CLI 0.1.18** and Next build help. No remote/latest documentation was needed; source pins remain unchanged and hooks remain enabled.

Fixture: `http://127.0.0.1:4326/workspace`, headed worker CLI session `ops-running4326`, existing server/data, owned `out-design-ops/`. The existing Follow system preference is exercised through media emulation and checked against the real root class/color scheme; no injected theme class or saved appearance change. The count is **6** and the accessible name remains **6 sessions running**. Authenticated read-only test stats are identical before/after, provider requests **4→4**. No save, send, reseed, engine action or other fixture/output change.

Live docs-first evidence: own session `01a07c1c-cf2e-73e1-bbe3-e758c8363042`, exact worktree, PreToolUse **10384** / PostToolUse **10368** in `/Users/mohamedadan/.codex/hooks/ops-docs-first-audit.jsonl`. Hooks remained enabled.

| Command | Exit/result |
| --- | --- |
| `CODEG_EXPORT_DIR=out-design-ops pnpm exec next build` | **0**, 33 static pages; final `design-running-badge-group-build.log` |
| `pnpm exec tsc --noEmit` after build | **0**; final `design-running-badge-group-tsc.log` |
| `pnpm exec vitest run src/components/conversations/sidebar-conversation-list.test.tsx` | **0**, all **43 existing tests**; final `design-running-badge-group-tests.log` |
| `pnpm exec eslint src/components/conversations/sidebar-conversation-list.tsx src/components/conversations/sidebar-folder-group-header.tsx` | **0**; `design-running-badge-group-eslint.log` |
| `playwright-cli -s=ops-running4326 --raw run-code --filename=reports/design-running-badge.playwright` | **0** before/after for each badge; six states each |
| Same CLI with `design-running-badge-group-setup.playwright` / `design-running-badge-group-cleanup.playwright` | **0** each; browser-only response overrides removed |
| `node reports/design-running-badge-report.mjs reports/design-running-badge-before.raw` and corresponding folder/group `after.raw` / `before.raw` | **0** each; Design Studio results above |
| `node /Users/mohamedadan/projects/design-studio/scripts/design-lint.mjs` with each changed component path | **0**, source-only diagnostics below |
| `git diff --check`, clean check/fetch/branch creation, initial commit/push and `gh pr create --draft` to main | **0** |

Existing tests cover sidebar rendering, keyboard focus/paging, group running counts and zero-count omission; none were changed or added. Final build/typecheck/43 tests/lint were rerun after the group correction; initial successful logs remain. No unrelated full-suite/Rust rerun for these two CSS utilities. Logs are local ignored files; scripts, raw results, screenshots and report are committed.

Design Studio aesthetic/a11y methods were applied sequentially inline, with no extra agent or paid executor. Badge Color/Detail and Accessibility last-mile micro-label contrast checks pass; default brief tokens and the inherited amber running meaning remain. The [source linter](design-running-badge-lint.raw)'s low **MOT-9** advisory cannot see generated Tailwind media rules: [actual rebuilt CSSOM](design-running-badge-hover-guard.raw) shows this exact folder-hover selector under **`(hover: hover)`**. No hover-guard fix is needed. [Scoped review](design-running-badge-review.json) records the disposition. This is a badge verdict, not full-app, every custom theme, mobile/native or motion certification; root's final reviewer remains independent.

The isolated [group source scan](design-running-badge-group-lint.raw) also flags loading/disabled/empty states in the header module. Those generic whole-component diagnostics are outside the noninteractive count correction: the parent owns list loading/empty presentation, while rename/delete actions were not exercised or certified here. They are preserved, not represented as a clean whole-component audit.

Keep **4326** running for review, serving **out-design-ops/** at **7f3fce05** with the existing server/data and nonsecret test token **ops-design-synthetic-operator**. Worker browser work is finished and the original real-server lists are restored. The inherited fixture-folder 404 diagnostics and unavailable Pi warning remain outside this seam. No live provider/inference request or change to another fixture/output.
