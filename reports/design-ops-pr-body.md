Changing language in a separate Settings tab no longer unmounts the workspace or loses unsaved Ops replies, private notes and complete review edits. The initial boot guard and backend-isolated in-memory state remain intact. Ops Back arrows and email/content direction support RTL; account/duplicate connection copy is removed; terminal explanations now follow actual receipts and distinguish provider acceptance from recipient delivery.

Selected-row metadata now meets measured contrast in both themes. Actual open threading fields pass without a product color change. The original failing probe, direct visibility/styles evidence and corrected real-theme recheck are retained. No sender, approval, API/DTO, component-prop, migration, lockfile or dependency change; the only Rust addition is a synthetic manual browser fixture.

Validation: 92 integrated frontend tests, typecheck, scoped ESLint and separate static export pass. Desktop/server/MCP checks, Clippy with warnings denied and 167 Rust Ops tests passed on unchanged Rust source. Actual CLI locale/RTL/terminal recovery checks preserve private edits and keep provider count 4→4. The final 12 scoped cases have 468 measured text samples and zero contrast failures; inherited reduced-motion/token-scanner limitations remain documented.

Production/evidence checkpoint: 8f0da7d6279ef016d8ff7d4b91cf2fc34b25e8c5. Final integrated code: 66d164e9fceded954ab3b8d2d0fe6d3a3617dd55, including accepted main 180be589 and PR12. Only additive NOTICE/export-ignore conflicts needed resolution; both complete additions survive, merged typecheck and merge-tree pass. Per root, the tested 4326 export remains unchanged and no Rust gates were repeated for this metadata-only resolution. Source mapping, stable fixture access and limits: reports/design-ops-fixes.md. Ready for root review; keep draft, no worker merge.
