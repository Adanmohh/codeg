# Independent GitHub issue phone review

Root Playwright CLI session `ops-issue-independent`, final product347e06d3 and
report head71048a5a, real protected4323 router and loopback-only providers.
No mocked browser API, live account, external issue or model prompt.

- Opaque created link redirected to protected login; valid synthetic token
  returned to the exact source and four evidence summaries/hashes plus full
  repository/title/body/labels. Approval disabled until confirmation.
- Confirmation survived390→1280→390. Keyboard Enter on the approval control
  created issue1: GitHub POST0→1, actual issues0→1, Telegram unchanged3.
- Unknown fixture committed issue2 then returned ambiguity. No create button
  remained; Check existing issue reconciled the receipt. POST count stayed2,
  actual issues2, Telegram3: no resend.
- Reloading the used link removed payload and decision actions. Document width
  equals390 with no horizontal overflow. Root inspected pending and reconciled
  screenshots. Worker separately covers rejection, invalid login, themes and
  complete inner-body scrolling; root does not relabel those as independent.

[pending](pending-mobile.png), [created](created-mobile.png),
[unknown](unknown-mobile.png), [reconciled](reconciled-mobile.png).
The expected initial unauthenticated console event is not a zero-console claim.
Final integrated Design Studio and real remote phone connectivity remain separate.
