These RSA keys were generated solely for local synthetic tests. They have never
belonged to a GitHub App, installation or account and must never be used outside
the fixture server. Tests verify RS256 with this public key; no actual service
credential or runtime keyring is loaded.
